<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{ title || 'CourseHub' }}
    </title>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.bunny.net" />
    <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />

    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Scripts and Styles -->
    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>

  <body class="font-sans antialiased">
    <div class="min-h-screen bg-gray-100 dark:bg-gray-900 flex flex-col">
      <!-- Include the Navigation Component -->
      @include('pages/components/navigation')

      <!-- Page Content -->
      <main class="flex-grow">
        {{{ await $slots.content() }}}
      </main>

      <!-- Footer -->
      @include('pages/components/footer')

      {{-- <footer class="bg-white dark:bg-gray-800 border-t border-gray-100 dark:border-gray-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
          <div class="flex justify-between">
            <div>
              <h3 class="text-lg font-bold text-gray-800 dark:text-white">
                CourseHub
              </h3>
              <p class="text-gray-600 dark:text-gray-400">
                Expand your knowledge with our courses
              </p>
            </div>
            <div class="flex space-x-6">
              <a href="#" class="text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300">About</a>
              <a href="#" class="text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300">Contact</a>
              <a href="#" class="text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300">Terms</a>
              <a href="#" class="text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300">Privacy</a>
            </div>
          </div>
          <div class="mt-4 text-center text-gray-600 dark:text-gray-400">
            &copy; {{ new Date().getFullYear() }} CourseHub. All rights reserved.
          </div>
        </div>
      </footer> --}}
      
      <!-- End of footer -->
    </div>
  </body>

</html>
