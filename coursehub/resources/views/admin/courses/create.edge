<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      Create Course
    </title>
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}" />
  </head>
  <body>
    <div class="container mt-5">
      <h1>
        Create New Course
      </h1>
      <form action="{{ route('admin.courses.store') }}" method="POST">
        @csrf
        <div class="mb-3">
          <label for="nama_kursus" class="form-label">Course Name</label>
          <input type="text" class="form-control" id="nama_kursus" name="nama_kursus" required />
        </div>
        <div class="mb-3">
          <label for="durasi" class="form-label">Duration</label>
          <input type="text" class="form-control" id="durasi" name="durasi" required />
        </div>
        <div class="mb-3">
          <label for="instruktur_id" class="form-label">Instructor</label>
          <select class="form-select" id="instruktur_id" name="instruktur_id" required>
            <option value="">
              Select Instructor
            </option>
            @foreach($instructors as $instructor)
              <option value="{{ $instructor.id }}">
                {{ $instructor.nama }}
              </option>
            @endforeach
          </select>
        </div>
        <div class="mb-3">
          <label for="biaya" class="form-label">Cost</label>
          <input type="number" class="form-control" id="biaya" name="biaya" required />
        </div>
        <button type="submit" class="btn btn-primary">Create Course</button>
      </form>
      <a href="{{ route('admin.courses.index') }}" class="btn btn-secondary mt-3">Back to Courses</a>
    </div>
    <script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}">
      
    </script>
  </body>
</html>
